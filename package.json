{
  "name": "agentic-prezi",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=26" },
  "scripts": {
    "test": "node --test",
    "audit:deps": "node scripts/audit-deps.mjs",
    "scan:secrets": "node scripts/scan-secrets.mjs"
  }
}
