# Secrets — never commit (this repo is PUBLIC)
.env
.env.*
*.pem
*.key
secrets/
.hermes/

# Dependencies / build
node_modules/
dist/
build/
*.log

# Local agent/tooling config — not part of this project, may hold local settings
.qwen/
.claude/

# OS / editor
.DS_Store
.idea/
.vscode/

# audit registry cache
.cache/
# release image tarballs (used in #4)
releases/

# Un-ignore the secret-scanner scripts themselves (global gitignore has *secrets* pattern)
!scripts/scan-secrets.mjs
!scripts/scan-secrets.test.mjs
